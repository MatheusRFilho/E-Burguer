import './footer.css';

const Footer = ({ activeItem }) => {
  return (
    <footer className="footer">
      <p className="footer-text">Desenvolvido por E-Burguer</p>
    </footer>
  );
};

export default Footer;
